from setuptools import setup

setup(name='active_sampling', version='0.0.1', packages=['active_sampling'], python_requires='>3.6',
      install_requires=[ 'matplotlib',
                        'numpy']
      )